import("//kernel/liteos_m/liteos.gni")

module_name = get_path_info(rebase_path("."), "name")
module_switch = defined(LOSCFG_BOARD_DEV_WIFI_A)
kernel_module(module_name) {
  #deps = [ "$product_path/hdf_config:hdf_hcs" ]
}
